# 上线部署说明：GitHub + Render

## 1. 解压 ZIP

解压后确认项目根目录直接包含：

```text
server.js
package.json
public/
```

## 2. 上传 GitHub

新建 GitHub 仓库，例如：

```text
belcanto-score-search
```

把项目文件上传到仓库根目录。正确结构：

```text
server.js
package.json
public/
README.md
SOURCES.md
```

错误结构：

```text
belcanto_score_search_v5/server.js
belcanto_score_search_v5/package.json
```

如果上传成错误结构，也能补救：在 Render 的 Root Directory 填 `belcanto_score_search_v5`。

## 3. Render 创建 Web Service

Render Dashboard → New → Web Service → 连接 GitHub 仓库。

配置：

```text
Runtime: Node
Build Command: npm install
Start Command: npm start
```

不要选择 Static Site，因为本项目需要 `server.js` 后端代理检索和下载公开 PDF。

## 4. 部署完成后测试

打开：

```text
https://你的项目名.onrender.com/api/health
```

如果看到：

```json
{"ok": true}
```

说明后端正常。

再打开首页：

```text
https://你的项目名.onrender.com/
```

## 5. 免费版注意

Render 免费 Web Service 长时间无人访问后可能休眠。第一次打开可能较慢，等 30 秒到 1 分钟刷新即可。正式长期使用建议升级实例或用定时监控工具保持唤醒。
