# 美声谱库聚合检索网站：上线部署说明

这个项目不是单纯的 HTML 静态网页，而是：

- `public/`：前端页面
- `server.js`：Node.js 后端检索代理

所以它可以本地运行，也可以部署成公开网址。

---

## 一、本地运行

### 1. 安装 Node.js

建议安装 Node.js 18 或更高版本。

安装后打开 CMD / PowerShell 检查：

```bash
node -v
npm -v
```

### 2. 解压 ZIP

解压后进入项目目录：

```bash
cd belcanto_score_search
```

### 3. 启动

```bash
npm start
```

浏览器打开：

```text
http://localhost:8787
```

Windows 也可以直接双击：

```text
启动网站_windows.bat
```

---

## 二、发给别人用的方式

### 方式 A：发 ZIP

你可以直接把 ZIP 发给别人。

对方需要：

1. 安装 Node.js；
2. 解压 ZIP；
3. 双击 `启动网站_windows.bat`；
4. 打开 `http://localhost:8787`。

缺点：每个人电脑都要安装 Node.js。

---

### 方式 B：部署成公开网页，推荐

部署成功后，你只需要发一个网址给别人，例如：

```text
https://你的项目名.onrender.com
```

别人不需要安装任何东西。

推荐平台：Render 或 Railway。

---

## 三、Render 部署步骤，推荐新手用

### 1. 创建 GitHub 仓库

1. 登录 GitHub；
2. 新建一个仓库，例如 `belcanto-score-search`；
3. 把解压后的 `belcanto_score_search` 文件夹里的所有文件上传到仓库根目录。

上传后的仓库结构应该是：

```text
server.js
package.json
public/index.html
public/styles.css
public/app.js
README.md
SOURCES.md
```

注意：不要把外层文件夹再套一层上传，否则 Render 可能找不到 `package.json`。

### 2. 登录 Render

进入 Render 控制台，创建 Web Service。

### 3. 连接 GitHub 仓库

选择刚才的 `belcanto-score-search` 仓库。

### 4. 填写部署参数

```text
Language: Node
Build Command: npm install
Start Command: npm start
```

项目里的 `server.js` 已经使用：

```js
process.env.PORT || 8787
```

所以可以自动适配云平台分配的端口。

### 5. 部署完成后复制网址

Render 会生成一个网址。把这个网址发给别人即可。

---

## 四、Railway 部署步骤

1. 登录 Railway；
2. New Project；
3. Deploy from GitHub Repo；
4. 选择你的项目仓库；
5. Railway 会自动识别 Node 项目；
6. 如果需要手动设置启动命令，填：

```bash
npm start
```

部署完成后，生成公开域名，复制给别人即可。

---

## 五、不推荐直接用 Netlify Drop 的原因

Netlify Drop 适合纯 HTML/CSS/JS 静态网站。

这个项目需要 `server.js` 后端去统一检索多个谱库，否则会遇到浏览器跨域限制，无法把不同网站的结果稳定聚合在同一个页面里。

如果只把 `public/` 拖到 Netlify Drop，页面能打开，但完整检索功能不稳定或不可用。

---

## 六、你发给别人的推荐话术

可以这样发：

> 这是我做的一个免费美声谱库聚合检索网站，可以同时搜索 IMSLP、CPDL、Art Song Central、Opera Arias、LiederNet、Internet Archive 等资源。能直接解析到 PDF 的会显示“直接下载 PDF”，如果原站限制直链，就会跳转到原站下载。打开这个链接即可使用：你的网址

---

## 七、常见问题

### 1. 搜索很慢怎么办？

因为它会同时请求多个国外谱库，部分网站响应慢是正常的。

### 2. 为什么有些结果不能直接下载？

有些网站不提供稳定 PDF 直链，或需要用户在原站选择版本、调性、声部、版权状态，所以只能跳转原站。

### 3. 能不能以后加更多网站？

可以。继续在 `server.js` 里增加新的 source 搜索函数，然后在前端展示即可。
